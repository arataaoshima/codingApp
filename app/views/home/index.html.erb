<div class="text-center page-title">
  <h1>Your Articles</h1>
</div>
<% current_user.articles.each do |article| %>
  <%= link_to article.title, article_path(article), class:"btn btn-outline-success" %>
<% end %>
